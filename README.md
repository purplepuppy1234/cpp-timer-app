# C++ Timer App ⏰

A simple countdown timer written in C++ that plays a loud beep when time’s up.

## Features
- User sets countdown in seconds
- Displays remaining time
- Plays a beep (Windows: `Beep()`, Linux/macOS: terminal bell)

## How to Run
```bash
g++ main.cpp -o timer
./timer

